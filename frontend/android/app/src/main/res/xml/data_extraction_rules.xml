<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing this app stores may leave the device in a backup.

  The session lives in the Android Keystore (capacitor-secure-storage-plugin)
  and the WebView keeps its own caches of boutique data — customer names, order
  values, photographs. A cloud backup would copy all of it to a Google account,
  and a restore would put one boutique's data on whatever device signed in next.
  allowBackup="false" covers Android 11 and below; this file is what covers 12+.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
