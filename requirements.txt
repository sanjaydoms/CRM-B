asgiref==3.11.1
Django==6.0.6
django-cors-headers==4.9.0
django-tenants==3.10.1
djangorestframework==3.17.1
pillow==12.2.0
psycopg2-binary==2.9.12
# Image uploads. Every upload path goes through default_storage, and
# STORAGES['default'] is MediaCloudinaryStorage whenever CLOUDINARY_URL is set
# -- see the comment in boutique_crm/settings.py for why the local filesystem
# was never viable on a hosted dyno.
cloudinary==1.46.2
django-cloudinary-storage==0.3.0
sqlparse==0.5.5
requests
gunicorn
# Test-only. Django's parallel runner pickles tracebacks across worker
# processes and cannot do it without tblib -- a failure under `--parallel`
# then reports "cannot pickle 'traceback' object" instead of the actual
# assertion, which sends you looking for a bug in the runner rather than in
# the test. Nothing in the application imports it.
tblib==3.2.2
